spring:
  datasource:
    url: jdbc:h2:file:/home/muramidara/IdeaProjects/Learning Java/Hotels-Api/hotels-api/src/test/resources/database/test;
#    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      schema-locations: classpath:sql/schema.sql
      data-locations: classpath:sql/data.sql
      mode: always
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true